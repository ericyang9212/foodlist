# 更新紀錄

重要改動的摘要（完整歷史見 `git log`）。

## 2026-08-20

### 地點細到鄉鎮：表單改收鄉鎮區，並建好地圖亮點要用的座標表
目標是讓足跡地圖不要只有 22 塊縣市色塊（30 筆足跡只點亮 6 個縣市，看起來很單調）。做法是在縣市底色上再畫「鄉鎮亮點」——亮點不需要 GPS，只要鄉鎮名字加一張預先算好的座標表。這次先做資料與表單，地圖本身還沒改。

- **新增 `scripts/generate-tw-areas.mjs`**：下載 g0v/twgeojson 的鄉鎮界線（20MB，只在腳本裡用、不進 repo），產出兩個檔：
  - `src/lib/twAreas.ts`（4.4 KB）：367 個鄉鎮市區名稱，依縣市分組，給表單下拉用
  - `src/lib/twTownPoints.ts`（16.6 KB）：每個鄉鎮在 `TaiwanMap` 的 `viewBox 0 0 1000 1295` 裡的座標，給之後的亮點用
- **座標怎麼算的**：從 `TaiwanMap.tsx` 的 22 個縣市 path 反推形狀重心，跟 GeoJSON 的真實經緯度重心配對，用最小平方法擬合「經緯度 → SVG 座標」的仿射轉換。本島 19 個縣市校正，**平均誤差 3.5 px、最大 20.4 px**（基隆最大，它形狀小又臨海）。驗證方式是把 377 個鄉鎮投影回去、檢查有沒有落在自己縣市的形狀裡：**全國 95.2%，實際有資料的 6 個縣市 136/136 全中**。落在外面的 18 個全是離島與被簡化 SVG 吃掉的迷你行政區，產表時已往形狀內修正
- **來源資料的三個坑都處理掉了**（都是數字對不上才發現的）：
  - `安平區(海)`、`梧棲鎮(海區` 這種海域多邊形要併回本體，不能各列一筆
  - 桃園還是 2014 升格前的「桃園縣／中壢市」，邊界沒變、名稱與後綴要換成「桃園市／中壢區」；員林鎮、頭份鎮同理（2015 升格縣轄市）
  - **高雄兩個「三民」被併成一筆**：市區的三民區與山上的舊三民鄉（今那瑪夏區）。用同 repo 的村里檔驗證出 91 個里分成兩群（民族／民權／民生 3 個里在山上），拆開後高雄才是正確的 38 個區
  - 少的 1 個是金門代管的烏坵鄉，來源資料沒有，已在腳本註明
- **表單改收鄉鎮**：`QuickLogSheet`、`QuickAddRegularSheet` 加「鄉鎮區」下拉（依已選縣市過濾，所以是 20 選 1 不是 367 選 1；換縣市會清掉已選的鄉鎮；海外／其他沒有清單就停用）。`QuickLogInput` 加 `area`，`App.tsx` 的 `handleQuickLog` 把它同時寫進食物的店家與足跡
- **`RestaurantsEditor` 的鄉鎮從自由輸入改成下拉**：這個原本不在範圍內，但不改會壞掉——地圖靠鄉鎮名字查座標，打「北港」而不是「北港鎮」就查不到。實際資料也已經歪了：45 家店有 8 家填了鄉鎮，其中 3 家是壞的（`北港`、`西`、`竹北`）。舊的自由輸入值會保留成選項（標「舊資料」），改存其他欄位時不會被清掉
- **既有資料的補法**：原本打算靠店名用猜的，實際上 30 筆裡只有 2～3 筆猜得出來。改用另一條路——短網址 `maps.app.goo.gl` 在瀏覽器裡因為 CORS 讀不到轉址目標，但在本機 shell 可以跟隨，轉址後的網址帶著完整地址。**足跡 29/30、餐廳 41/45 判定成功**，剩下的是沒存連結的（老宅咖啡、三家海底撈）
- **補資料已寫進正式庫**（2026-08-20）：足跡 29/30、餐廳 41/45 補上鄉鎮，並把 3 筆壞掉的自由輸入值改正（`北港`→北港鎮、`西`→西區、`竹北`→竹北市）。剩下沒補的是沒存地圖連結的那幾筆（足跡的老宅咖啡；清單的老宅咖啡與三家海底撈）
- 寫入前修掉自己產的一個 bug：原本的 `jsonb_agg` **沒有 `order by`**，Postgres 不保證聚合順序，而 `restaurants[0]` 決定「店家型 vs 想吃型」——順序一亂食物就會被分錯類。改成 `with ordinality` + `order by ord`，並先用唯讀查詢對最複雜那筆（4 家餐廳）比對過順序與名稱才執行
- 寫入後驗證：45 家餐廳一家不少、0 個壞掉的陣列或元素、`restaurants[0]` 全部沒變、19 個縣市/鄉鎮組合全部是合法名稱且查得到地圖座標

### 台灣地圖加上鄉鎮亮點
- `TaiwanMap` 新增可選的 `townPoints`：在縣市色塊之上畫金色亮點，同一個鄉鎮的店共用一個點，點的大小用**平方根**縮放（讓面積跟次數成正比，不然次數多的會誇張地大）。點可以點，點了等同點該縣市
- **亮點一定要有深色外環，這不是裝飾**：縣市色塊愈亮代表足跡愈多，而金色 `#e2c08d` 壓在最亮的色塊 `#f0beac` 上只有 **1.04:1**——等於隱形，偏偏那正是最需要看見亮點的縣市。加上近黑外環後實測：環 vs 最暗縣市色 3.99:1、vs 最亮 11.42:1、金核心 vs 環 10.96:1，全部過 3:1（WCAG 非文字對比門檻）
- `FoodprintsPage` 算出鄉鎮亮點：查不到座標的（舊的自由輸入值如「北港」）自動略過不畫，不會壞掉也不會亂標
- **這次用拋棄式預覽入口實際渲染過**（假資料用的是解析出來的真實鄉鎮，等於補完資料後的樣子），量了 DOM 幾何也轉成圖檢查過，檢查完預覽檔已刪除。結果：15 個亮點正確、沒填鄉鎮的那筆正確地沒有點
- **發現一個既有問題（不是這次改出來的）**：地圖容器是 375×280，但 viewBox 為了容納金門（最左）與馬祖（最上）撐到 1000×1295，導致台灣只縮到 0.216 倍、寬 216px，左右各留 80px 全黑。都會區的鄉鎮因此擠在一起（台中西區與南區的圓心在螢幕上只差 1.4px）。量過三種改法的效果，已提給使用者選，**程式碼維持原樣沒有動版面**

## 2026-08-16（傍晚）

### 跑馬燈改成留言板：只輪播最新 5 則，舊的留成紀錄
- 問題：原本整個跑馬燈就是 `marquee` 單一列裡的一段多行文字，一行一則。訊息只會愈疊愈多，想清掉就得刪那一行——刪了就什麼都不剩。而且兩人同時編輯會整段互相蓋掉（後寫的贏）
- 新增 `marquee_messages` 資料表（一則一列，含建立時間），RLS 與其他表相同（登入者全權），並加入 realtime。原本那 2 行已搬進去，順序依對話語意判定（「新版本如何～～」較早、「字好粗好友善老人家」較新）
- 跑馬燈只輪播**最新 5 則、新到舊**；更舊的不刪、不過期，留在留言板裡永遠翻得到
- 點跑馬燈打開的畫面從「編輯文字」變成留言板：上面留新訊息（送出失敗不清空輸入框），下面是完整紀錄（附日期、可單筆刪除），最新 5 則標「顯示中」。顏色與速度設定保留，改動時才出現「儲存」
- 兩人同時留言不再互蓋——各自新增一列，realtime 讓對方的留言立刻出現
- `marquee.text` **保留不動**當作舊資料備份，程式已不再讀它
- 輸入框不放提示字（原本的「想跟對方說什麼？」拿掉）——上面已經有「留一則訊息」的欄題，再放一句就是重複
- 未做：不標「誰留的」（依你的選擇），也沒有自動過期或封存


### 抬頭只留字標
- 拿掉「待吃清單」大標，字標 `PSJ DICE LIST` 從 12px 的灰色眉標升成 `<h1>`（34px 襯線）——頁面仍要有一個標題，而且原本那行小灰字撐不起版面
- 實測寬度 226px、抬頭可用寬度約 274px，單行不換行也不會撞到右上角的公告鈴

### 跑馬燈改成真的「從右邊跑進來」
- 上一版為了修「短句同時顯示兩份」，把塞得下的句子改成置中靜止——結果變成沒有動態、訊息直接出現，反而更怪
- 改成標準跑馬燈：每一則都從容器右緣外進場、往左走到左緣外，走完換下一則。位移量由元件量出容器寬與字寬後用 CSS 變數帶進 keyframes，所以不管句子多長都剛好完整進場、完整離場，也不需要複製第二份（重複顯示的問題自然消失）
- `speed` 的意義變明確了：就是「跑完一趟的秒數」（8 快、60 慢）。多則訊息不再用計時器輪替，改成一則跑完才換下一則（`animationiteration`），淡入淡出的 `.animate-mqfade` 隨之移除
- 「減少動態」偏好下維持置中靜止、多則用計時器輪替（沒有動畫可以接力）
- 用無頭瀏覽器逐幀確認過：起點 `--mq-from` = 容器寬 402px（文字左緣剛好在右緣外），終點 `-153px`（整句走完），x 座標實測單調左移

### 收尾：全站巡一遍，修掉三個真的壞掉的地方
這次第一次用無頭瀏覽器把主畫面實際渲染出來看（用假資料的拋棄式預覽入口，檢查完已刪除），才抓到前兩個問題：

- **彈窗的關閉 ✕ 跑到左上角**：`.icon-btn` 的 `position: relative` 打贏了 Tailwind 的 `absolute`。根因是自訂類別沒有分層，而未分層的 CSS 會贏過任何 `@layer` 裡的規則。把元件類別整批收進 `@layer components` 後，utilities 才能正常覆寫——同一個坑也讓 `.t-caption` 的顏色蓋掉 `text-*`，之前是靠 `!important` 硬壓，現在那些 `!` 都拿掉了
- **跑馬燈短句會顯示兩次**：無縫捲動是靠複製一份接在後面，句子短到塞得下時兩份會同時出現，看起來像訊息重複。改成先量寬度，塞得下就置中靜止、不捲動（用 ResizeObserver，因為字體是非同步載入、寬度會變）
- **地圖上「去過一次」和「沒去過」幾乎同色**（對比 1.85），色階暗端提亮後拉到 3.22
- 一併清掉換版遺留：使用說明還在講「中間金色按鈕」「清單右上角的靈感匣」等三頁時代的操作（已改寫成一頁三段落）、公告內文寫死的舊襯線字、`FoodCard` 與 `index.css` 的過時註解、PWA manifest 還停在舊底色 `#0a0a0a`、以及沒人用的 token（`--r-sm`／`--r-lg`／`--color-amber-soft`）與和 Tailwind 內建重複的 `.tabular-nums`

### 字體換成襯線大標，點題色做成金屬漆
- 字體改成兩套：大標題走襯線（Playfair Display 配 Noto Serif TC，Latin 與中文各自吃對的字），介面與內文維持系統無襯線。整頁都襯線會變閱讀負擔，所以只套在 `.t-display` / `.t-title` / `.text-hero`——連跑馬燈也一起變襯線大字
- 玫瑰金 / 香檳金 / 丁香紫鋪在色塊上時改成金屬漆漸層（亮邊→本色→暗面→反光四段）＋ 頂緣白底緣暗的內陰影，模擬光打在弧面上；套用在主要按鈕、選中的 chip、浮動新增鈕、logo 底板
- 文字沒有做金屬漸層：漸層字在強制對比模式下會整段消失，這條底線不動
- 漸層的每一個色階都實測過對比（深色文字壓在上面最低 5.93:1），不是只算中間那段

### 跑馬燈字放大成滿版粗體
- 字級從 15px / 600 改成 `.t-title`（22px / 700），左右邊緣柔化從 32px 收到 20px、內距從 24px 收到 12px，讓字幾乎貼著兩側跑
- 整條列的高度**刻意不動**（safe-area + 56px）：`HomePage` 的段落切換列就吸在這個 56px 底下，改高度得連那邊的常數一起改，已在程式碼註明

### 「今晚吃什麼」按鈕放大
- 前一版把它從大卡片收成小 chip，結果太不起眼；它是全 app 最常按的動作，字級改成 `.t-heading`（13px → 17px）、內距與圖示一起放大，但仍維持按鈕而不是回到整張卡片

### 底部導列拿掉，新增改成浮動按鈕
- 為什麼：三頁併成一頁後，那條底列只剩一顆按鈕，卻仍佔掉整條版面寬度
- 改成右下角的浮動膠囊鈕「＋ 新增」：不佔版面、拇指搆得到，是全頁唯一發光的元素（玫瑰金光暈）。清單空狀態的文案跟著從「點下方 +」改成「按右下角『新增』」

### 配色換成暖調深色 + 玫瑰金
- 上一版的明亮蘋果版面改成暖近黑畫布（`#131010`，刻意帶紅，純灰會讓玫瑰金顯髒）+ 微暖深色卡片；版面骨架（大圓角、毛玻璃、分段控制項、收緊字距）維持不動，只換色彩
- 點題色改成同一個暖色家族：互動與清單用玫瑰金、足跡用香檳金、靈感匣用丁香紫（唯一的冷調對比）；背景光暈同步換成玫瑰金／香檳金，並加回極細顆粒讓暗部不死平
- 改成單一深色主題，不再跟隨系統切淺色——玫瑰金要壓在深色上才成立
- 因為上一版已把顏色收斂成 token，這次主要只改 `index.css`；但仍有幾處必須手動重挑：跑馬燈顯示色、台灣地圖色階（改玫瑰金階）、分段控制項（深色要「深軌道＋亮滑塊」，照淺色寫法會反過來變暗）、深銅色 logo 在深底會糊（主畫面給玫瑰金底板、全螢幕的加柔光暈）
- 對比一樣重算過：文字最低 5.3:1，三個點題色與其深色淡底、以及深色文字放在亮色塊上（含浮動鈕漸層的每一個色階）全部 ≥ 4.5:1。`npx impeccable detect src` 維持 0 issues

## 2026-08-16（下午）

### 全站改版：暖黑金 →「蘋果版面」明亮風格
- 為什麼：舊的暖黑底 + 香檳金 + 襯線字走的是「精品編輯」路線，質感重但閱讀吃力、也不像日常隨手用的 app。改成明亮、通透、圓潤的蘋果版面
- 色彩系統重寫：`src/index.css` 的 `@theme` 定義全部 token（畫布 / 表面 / 分隔線 / 文字 / 點題色），元件改用語意類別（`bg-surface`、`text-muted`、`border-separator`…），不再寫死 hex。順帶讓深色模式只要翻 token 就成立
- 不只黑白灰：互動色統一藍 `tint`，三個段落各有點題色（足跡松綠、清單珊瑚、靈感匣紫），評分琥珀、刪除紅；畫布再鋪一層極淡的三色光暈，大片淺灰不至於死平
- 字體與排版：改用系統字（SF Pro / 蘋方，退到 Noto Sans TC），字距從「拉開」改成「收緊」，全站約 190 處 `tracking-*` 清掉；字級階梯對齊 iOS 的 Large Title / Title / Headline / Body / Footnote
- 元件語彙：圓角放大、卡片改白面淺陰影、吸頂與底部導列改毛玻璃（`.blur-bar`）、分頁改 iOS 分段控制項、狀態標籤改淡色底、地圖改松綠色階、抽籤主按鈕改珊瑚漸層
- 動畫：新增 `.animate-rise`（段落 / 內容進場）、`.stagger`（列表逐項錯開）、`.animate-pop`（抽到結果彈出）、曲線統一走 `--ease-ios` / `--ease-out-quint`。回彈過衝的曲線被 impeccable 判為反模式，已改成純減速
- 無障礙：淺色底的色票全部重算過對比（正文、次要灰、五個點題色的深淺版、反白文字在色塊上），最低 4.5:1；珊瑚色與 `fill` 底色都為此微調過。`npx impeccable detect src` 維持 0 issues
- 順手補上清單搜尋框「清除」鈕的 `aria-label`（上一輪提出、這次一起做）

## 2026-08-16

### 清單／足跡／靈感匣三頁合併成單一主畫面（以足跡為主軸）
- 為什麼：兩個底部分頁＋一個彈窗，同一批資料被切成三個入口，「剛吃完想記一筆順便看看清單」得跳三次。改成一頁三段落後，底部只剩新增按鈕
- 新增 `HomePage`：持有唯一的捲動容器、品牌抬頭、抽籤入口與段落切換列（足跡 / 清單 / 靈感匣，預設足跡）。切換段落會把捲動位置歸零，不會帶著上一段的位置落在半途
- `ListView`：拿掉自己的抬頭與捲動容器；分頁（想吃／嘗過／全部）與縣市篩選改由 `HomePage` 持有——抽籤後的「快速加吃過的店」要能把清單切到「嘗過」，狀態得在共同的上層。搜尋列吸頂位置改由上層算，才不會蓋掉段落切換列
- `InboxPage`：從全螢幕彈窗改成頁內段落（靈感詳細仍是覆蓋層）；未整理的張數改顯示在段落切換列的「靈感匣」上
- `FoodprintsPage`：頁標改成段落標題；時間軸拿掉內層 `max-h-[50vh]` 捲動，直接跟著整頁捲，手機上少一層「捲動區裡的捲動區」
- 「今晚吃什麼」大卡片縮成抬頭下方的一顆小按鈕（`.btn-secondary`），`TonightModal` 內容與抽籤邏輯完全沒動，只換入口；因為移到共用抬頭，三個段落都按得到
- 分包調整：足跡是預設段落所以隨主畫面靜態載入，清單與靈感匣改成進去才下載
- 移除只為底部分頁存在的 `Tab` 型別

### 維護畫面沒訊息就不顯示文案
- `MaintenanceScreen` 原本 `message ||` 會退回寫死的「正在進行維護，請稍後再回來。」，導致想留白時得在 `maintenance_message` 塞一個半形空白繞過
- 改成有訊息才渲染整段 `<p>`，且用 `trim()` 判斷——只有空白等同沒訊息，不會留下一段空 `<p>` 撐開版面

## 2026-07-21

### 修正：新增／編輯表單儲存失敗會關閉、內容全丟
- 根因：`AddEditPage` 呼叫 `onSave` 後不等結果就關閉，App 層也無條件關表單；網路一失敗，樂觀更新回滾＋表單已關，打到一半的筆記全部消失
- 改成 `onSave` 回傳成功與否：失敗時表單留在原地（內容不丟）直接重試，成功才由 App 層關閉；照片已上傳成功的話重試不會重傳第二份
- 兩處 `alert()`（新增頁圖片上傳失敗、靈感匣上傳失敗）改用全站統一的 toast

### 無障礙與互動品質（以 impeccable 稽核標準全站掃描）
- 對比拉到 WCAG AA：`#5d574c`（2.78:1）與 `#6d6557`（3.47:1）統一成 `#837b6e`（4.8:1）；眉標透明度 0.55/0.6 → 0.8/0.82
- 漸層字（`background-clip: text`）改實色 `#e0c68c`：原寫法在強制對比模式下標題會整段消失
- 圖示鈕點擊區補到 44×44（`::after` 撐熱區，版面不動）；補 18 個 `aria-label`
- 全域 `prefers-reduced-motion` 支援（先前只有跑馬燈處理）；表單控件納入鍵盤焦點金環；`transition: all` 改明確屬性（commit `86fa382`）

### 新增 `CLAUDE.md` 維護守則
- 行為守則（宣告與行動一致、範圍紀律、驗證＝執行）＋固定流程（編輯前重讀、驗證三連、記 CHANGELOG）＋專案關鍵事實（樂觀更新模式、照片存於靈感、店家型判別規則）＋設計系統底線；每次開新對話自動載入

## 2026-07-18

### 足跡頁「記一筆」：清單沒有的店也能直接記
- 足跡頁右上新增「＋ 記一筆」按鈕：填名稱、縣市、Google 地圖連結、吃的日期（預設今天、可補記）、照片、一句話感想
- 儲存時一併建立「嘗過」項目＋足跡紀錄，回訪抽籤、店家視角、足跡地圖立即生效；地圖連結內含座標會順手解析
- 任一步失敗（照片上傳／食物寫入／足跡寫入）會回滾前面已建立的資料，不留半套；sheet 留在原地可重試（commit `7c94476`）

### 修正：足跡卡片看不到照片
- 根因：照片常加在「食物」上（新增頁的截圖欄，存於靈感相簿），但足跡卡片只認記錄當下上傳的那張，看起來像照片消失
- 足跡沒有自己的照片時，退回顯示該食物的照片；既有足跡（如 7/17、7/18 那兩筆）立即補上，不用重傳（同 commit `7c94476`）

### 質感打磨：同風格提升整體 UI
- 冷灰全面暖化：全 app 一百多處純冷灰（`#0a0a0a`／`#161616`／`#1f1f1f`／`#2a2a2a`／`#555`／`#666`／`#777`…）整批遷移到帶香檳色偏的暖中性階，部分併回既有暖色 token；金色系不動。底色、邊線、灰字與香檳金融為同一世界
- 背景質地：`#root` 在頂部金暈外加極細顆粒（SVG 噪點 ~4%）與底部微弱餘燼光，暗部不再死平
- 細節工藝：原生控件走暗色系（`color-scheme: dark`）＋香檳 `accent-color`、輸入游標金色、日期選擇器圖示調暖、鍵盤焦點香檳細環、標題斷行 `text-wrap: balance`
- 六處空狀態「— —」統一升級為細金線＋✦ 的共用飾紋（`EmptyMark`）（commit `290fde4`）

## 2026-07-08

- 今晚吃什麼開啟時自動觸發抽籤動畫，與「再抽一個」一致（commit `8fedae5`）

## 2026-07-07

### 效能：程式碼分割（code splitting）
- 6 個非首屏元件改用 `React.lazy` 動態載入：`FoodprintsPage`、`AddEditPage`、`DetailPage`、`InboxPage`、`LogFoodprintSheet`、`AnnouncementsModal`，各使用點包 `<Suspense>`（足跡分頁 fallback 用 `FullScreenLoader`，彈窗類用 `null`）
- 主 JS 包從 531 kB 縮到 460 kB（gzip 151→131 kB），Vite 的 500 kB chunk 警告消失；最重的足跡頁（含台灣地圖 SVG，26 kB）改為點「足跡」分頁才下載
- PWA precache 自動涵蓋所有拆出的 chunk（24 entries），離線仍可開全部頁面
- 只動 `src/App.tsx` 的 import 方式，不碰業務邏輯（commit `a5b115f`）

### 修復 iOS Safari 自動縮放
- `index.html` viewport 加 `maximum-scale=1.0, user-scalable=no`，防止點輸入框或滑動時頁面突然放大
- 全站 18 個 input/textarea/select 字級統一改 `text-base`（16px）——iOS Safari 在 input 字級 < 16px 時會強制 zoom in
- `src/index.css` 加保底規則 `input, textarea, select { font-size: 16px; }`（commit `99d7920`）

### 其他
- 修正足跡樂觀新增插入順序，改依 `ate_at` 排序而非直接插最前面（commit `fc30fc1`）

## 2026-07-06

- 新增頁加「店家／想吃的東西」切換，解決打「壽司」被當成店的混淆
- 技術審視：修正錯誤處理缺口與 React 反模式
- 足跡時間軸新增月份折疊功能，最近 2 個月預設展開
- 足跡地圖改為台灣縣市 SVG 填色圖，取代 Leaflet（多次迭代：熱力圖 → 全屏抽屜 → 回歸正常捲動版面）
- 抽籤加「回訪」來源、快速加常去的店、縣市選擇提到最上面
- 跑馬燈改為永遠橫向滾動（無縫循環），字體換成 Noto Serif TC 斜體
